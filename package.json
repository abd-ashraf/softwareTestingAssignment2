{
  "scripts": {
    "test": "mocha --exit",
    "coverage": "nyc npm test -- --exit && nyc report --reporter=text-lcov > coverage/lcov.info"
  },
  "devDependencies": {
    "chai": "^4.3.10",
    "coveralls": "^3.1.1",
    "mocha": "^10.2.0",
    "nyc": "^15.1.0"
  },
  "type": "module"
}
