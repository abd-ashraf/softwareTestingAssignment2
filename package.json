{
  "scripts": {
    "test": "mocha --recursive ./test --exit",
    "coverage": "c8 --reporter=lcov --reporter=text mocha",
    "coverage-report": "c8 report --reporter=text-summary && c8 report --reporter=html"
  },
  "devDependencies": {
    "chai": "^4.3.10",
    "coveralls": "^3.1.1",
    "mocha": "^10.2.0",
    "mocha-lcov-reporter": "^1.3.0",
    "c8": "^7.0.0"
  },
  "type": "module"
}
